type IntegerVariable = {
  name: string;
  min: number;
  max: number;
};

type DecimalVariable = {
  name: string;
  min: number;
  max: number;
};

type StringVariable = {
  name: string;
  options: string[];
};
